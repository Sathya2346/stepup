package com.stepup.repository;

import com.stepup.model.ChatRule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChatRuleRepository extends JpaRepository<ChatRule, Long> {
}
